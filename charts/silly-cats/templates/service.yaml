apiVersion: v1
kind: Service
metadata:
  name: silly-cats-svc
  namespace: silly-cats-ns
spec:
  type: ClusterIP
  selector:
    app: silly-cats
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http-app